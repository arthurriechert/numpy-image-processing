# Main library for manipulating image matrix representations
import numpy as np

# Takes 3D, RGB array and does min-max stretch || Takes numpy array
# Check README.md for more info
def rgb_min_max_linear_stretch(np_array):
    np_array[2].split()
    print(np_array)